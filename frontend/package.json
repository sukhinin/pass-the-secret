{
  "name": "frontend",
  "scripts": {
    "dev": "parcel serve src/styles.css src/*.html",
    "build": "parcel build src/styles.css src/*.html",
    "postbuild": "version.sh dist/*.html",
    "clean": "rimraf dist .parcel-cache"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.4.0",
    "autoprefixer": "^10.4.2",
    "parcel": "^2.0.1",
    "postcss": "^8.4.5",
    "rimraf": "^3.0.2",
    "tailwindcss": "^3.0.7"
  }
}
